# Jarvis 个人 Agent

Jarvis 是面向个人多种任务的通用助理。笔记找回是其中一个使用场景，不代表项目的整体边界。

## Language

**Jarvis**：
协助用户完成不同类型个人任务的通用 Agent。

### 笔记找回场景

**笔记找回**：
根据用户对笔记中某个点的记忆，寻找对应的已有笔记内容。

**原文片段**：
已有笔记中的原始文字，是用户判断是否找对内容的依据。
_Avoid_: 生成的解释、改写摘要

**原笔记位置**：
能够定位回原始笔记中相关内容的位置，供用户核对原文。

### 上下文与状态

**任务**：
用户一次输入及其在当前会话内完成的多轮 Agent 工具调用过程；同一会话中的后续任务可以回查既有证据索引，但任务目标和进度彼此独立。

**会话**：
以会话标识区分的一段连续交互；消息历史和证据索引在会话内保留，并写入会话记录以便跨进程恢复。
_Avoid_: 进程、连接

**上下文**：
Jarvis 在每次模型调用时提供给模型的全部信息，包括稳定指令、工具定义、任务历史和工具结果。

**证据索引**：
会话内对已执行检索结果的紧凑目录，记录工具、规范化参数、来源位置、截断/复用状态及文件有效性；它指向原始工具结果，不代替原文。

**上下文压缩**：
当上下文估算接近窗口上限时，按切点把更旧的完整 turns（含助手正文）压成一条累积 checkpoint，并保留切点之后的近期原文；稳定指令、工具定义、消息协议和证据引用不被随意删除。

**压缩服务**：
执行上下文压缩的独立组件，对外一个入口；自动压缩、主动压缩和溢出恢复三种触发共用同一套切点、累积 checkpoint 与记录写入，产出相同的状态效果，只差触发原因。

**自动压缩**：
由可用输入预算判定触发的压缩，发生在模型调用之前。

**主动压缩**：
用户显式发起的压缩；默认退休比当前任务更旧的全部原文，可用 `/compact <token>` 指定本次保留窗口；仍受可用输入预算约束，且不构成一个新任务。

**溢出恢复**：
模型服务因上下文超限拒绝请求后，丢弃失败的回复、压缩一次并只重试一次的过程；服务端错误无法稳定识别时不启用。

**切点**：
压缩时保留原文的起点；优先落在任务边界（user 消息），任务内部退到轮边界（assistant 消息），tool 结果不单独切断，以保证 tool 消息与其 assistant tool_call 的配对完整。

**保留窗口**：
压缩后保留原文的近期内容预算；以 token 数控制，吸附到合法边界时允许略超。

**累积 checkpoint**：
压缩产出的结构化摘要，在本次新退休的 turns 基础上吸收上一次摘要，形成会话到当前为止的状态；活上下文只保留最新一条。

**归档**：
把切点之前的已结束对话段落从参与每次请求的原始消息转成累积 checkpoint，并把原文留在会话归档中的动作。
_Avoid_: 摘要压缩、汇总

**归档记录**：
一次压缩追加到会话日志的持久记录，记录 checkpoint、切点和被收走的调用标识；旧记录留在日志中供审计，活上下文只保留最新 checkpoint。

**会话归档**：
会话内保存但不发送给模型的原始工具结果及压缩记录，用于调试和必要时核对；随会话记录持久化，恢复后仍可回查。

**压缩模型**：
专门生成上下文摘要的模型配置；可以在 `.env` 中单独指定，未指定时使用主模型。

**模型窗口元数据**：
上游模型服务可能返回的上下文容量描述；它只在显式配置缺失时作为窗口估算来源，字段不存在或不可验证时不作为事实使用。

**证据引用**：
指向任务中已检索内容的可核对记录，至少包含来源文件及其位置；它用于让模型和用户回到原始内容，而不是替代原文的自由摘要。

### 会话持久化

**会话记录**：
一段会话的持久化事件日志，按发生顺序记录消息、任务边界、工具结果归档和上下文改写。

**会话标识**：
区分会话的可读时间戳加短随机后缀，用于列出候选和恢复指定会话。

**状态目录**：
保存会话记录的可配置目录，默认位于工作区之外，避免被 Jarvis 自己的文件工具读到。

**恢复**：
把既有会话记录加载为新进程的对话历史；恢复不重新执行中断的工具调用。
_Avoid_: 重放、续跑

**中断补齐**：
恢复时为缺少结果的工具调用补写的占位结果，标明执行状态未知；它不证明调用未执行，也不触发重新执行。

### 模型容量

**模型能力配置**：
与供应商和模型对应的容量描述，包括上下文窗口与输出上限，并带有可核对的来源和日期。

**上下文窗口**：
当前模型服务允许一次调用容纳的上下文容量；缓存命中的输入仍占用该容量。

**单次输出上限**：
用户允许模型一次调用最多生成的 token 数，受模型自身输出能力约束，与上下文窗口容量分别定义。

**可用输入预算**：
从当前模型上下文窗口中扣除预留后，可供输入使用的容量；若服务另有输入上限，还要受该上限约束。

**预留**：
从上下文窗口中为输出空间与估算误差保留的固定 token 量；压缩触发与拒绝发送共用同一个值。
_Avoid_: 安全余量（按窗口比例计算的旧概念，已被替代）

### 缓存测量

**缓存命中量**：
模型服务明确上报的本次输入中复用缓存的 token 数；服务端未上报时为未知，不能视为零。

**缓存 token 占比**：
缓存命中量占全部输入 token 的比例；跨调用汇总时使用总命中量除以总输入量，与有命中的请求占比不同。

### 记忆

**History**：
按天切分、按行保存时间戳的用户输入历史；它是用户陈述可读、可回查的索引，不等同于上下文压缩产生的会话归档。
_Avoid_: 冷数据归档、会话归档

**任务轨迹**：
按任务保存完整消息、工具调用、工具结果与状态变化的不可变原始记录；它是记忆事实来源的最终依据。
_Avoid_: History、上下文压缩摘要

**记忆事实**：
从 History 派生的、带来源与时间范围的用户相关陈述；新事实可以使旧事实失效，但不抹除旧事实或其来源。
_Avoid_: 覆盖后的记忆、无来源摘要

**记忆时间戳**：
附在 History 行和 SQLite 记忆记录上的事件时间与记录时间，用于确定事件顺序、事实有效期和冲突修复的依据。

**事实冲突**：
同一主体和属性在重叠有效时间内不能同时成立的两个事实；可信的新事实使旧事实失效，两个事实及其来源仍保留。

**推理冲突**：
由模型从证据得出的两个不一致结论；它不自动改变记忆事实，须保留推理依据并等待更多证据或复核。

**Recent**：
当前会话中每轮追加的完整近期信息，并直接参与模型上下文；它保留近期任务的细节，不是跨任务事实库。

**Pending**：
由移出 Recent 窗口的已结束任务异步提取、尚未固化的记忆候选；它不参与模型上下文，提取失败可从任务轨迹重试。

**Memory**：
带来源和有效时间、可跨任务检索的稳定用户事实；其结构化用户画像摘要写入 memory.md，作为模型上下文的固定前缀，并只在 consolidation 完成后更新。

**Memory DB**：
保存全部 Memory 事实、来源、时间范围、版本和检索索引的 SQLite 数据库；Memory 召回必须查询 Memory DB，不读取 memory.md。

**Memory Markdown**：
由 Memory DB 中当前有效事实生成的结构化用户画像摘要文件 memory.md；它只作为稳定上下文前缀和人工编辑入口，不是召回真源。

**混合记忆召回**：
对用户 query 先做查询重写，再融合关键词与可用的向量召回，按事实有效期过滤并去重后返回带来源的 Top-K 事实；无向量能力时关键词召回独立可用。

**稳定前缀**：
每次模型调用固定注入的 self.md 与 memory.md 内容；细粒度 Memory 事实通过混合记忆召回动态注入。

**HyDE 触发**：
有可用向量且首轮向量查询成功、混合召回有效事实少于 3 条或最高相关性低于阈值时，生成假设文本补充向量检索。

**动态记忆预算**：
单次请求最多注入 8 条细粒度事实，总量不超过 1,000 token。

**Agent Self Memory**：
写入 self.md 的 Agent 自身定位，包括角色、行为原则、能力边界和工具使用约束；它作为模型上下文的固定前缀。
_Avoid_: Agent Experience、项目知识

**Agent Experience**：
从任务中归纳出的可迁移工作策略；它不属于 Agent Self Memory。

**用户画像摘要**：
由稳定用户事实生成、具有固定栏目和 1,500 token 上限的 memory.md 内容；栏目为身份与背景、偏好与工作方式、沟通偏好、长期目标、持续约束和当前有效状态。它不取代 SQLite 中可回溯的完整事实。

**用户修正**：
用户对 memory.md 的直接编辑；它是高置信度事实来源，必须立即写回 SQLite、使冲突旧事实失效，并在下一任务前更新 memory.md。

**个人记忆系统**：
仅服务一个本地用户的记忆系统；它只保存该用户的稳定信息，不承担多用户隔离或项目知识管理。

**记忆准入**：
默认仅允许用户明确表达的稳定偏好、沟通方式、长期目标、持续约束和背景进入 Memory；临时计划与模型推断不自动晋级，敏感事实须用户明确要求。

**事实失效**：
结束事实的有效期并从用户画像摘要移除，同时保留事实及其来源的审计记录；彻底删除是独立操作。

**Pending 过期**：
候选事实在 30 天内没有新证据或晋级时标记为过期，不再参与 consolidation，但保留来源与判定理由。
